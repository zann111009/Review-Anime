"use client"; import { signIn } from "next-auth/react"; import { useState } from "react"; import Link from "next/link"; import { useRouter } from "next/navigation";
export default function Login(){const [error,setError]=useState("");const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await signIn("credentials",{login:f.get("login"),password:f.get("password"),redirect:false});if(r?.error)setError("Username/email atau password salah.");else router.push("/");}return <section className="shell flex min-h-[70vh] items-center justify-center"><form onSubmit={submit} className="glass w-full max-w-md rounded-2xl p-6"><h1 className="text-2xl font-bold">Welcome back</h1><p className="mt-1 muted">Login to rate and review anime.</p><label className="mt-5 block text-sm">Username or email<input required name="login" className="field mt-1"/></label><label className="mt-4 block text-sm">Password<input required name="password" type="password" className="field mt-1"/></label>{error&&<p className="mt-3 text-sm text-red-300">{error}</p>}<button className="btn btn-primary mt-5 w-full">Login</button><p className="mt-4 text-center text-sm muted">Belum punya akun? <Link className="text-violet-300" href="/register">Register</Link></p></form></section>}
