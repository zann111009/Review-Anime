"use client"; import { useState } from "react"; import { useRouter } from "next/navigation"; import Link from "next/link";
export default function Register(){const [error,setError]=useState("");const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(f)});if(!r.ok)setError((await r.json()).error||"Registration failed");else router.push("/login");}return <section className="shell flex min-h-[70vh] items-center justify-center"><form onSubmit={submit} className="glass w-full max-w-md rounded-2xl p-6"><h1 className="text-2xl font-bold">Create account</h1><label className="mt-5 block text-sm">Username<input required name="username" minLength={3} className="field mt-1"/></label><label className="mt-4 block text-sm">Email<input required name="email" type="email" className="field mt-1"/></label><label className="mt-4 block text-sm">Password<input required name="password" type="password" minLength={8} className="field mt-1"/></label><label className="mt-4 block text-sm">Confirm password<input required name="confirmPassword" type="password" minLength={8} className="field mt-1"/></label>{error&&<p className="mt-3 text-sm text-red-300">{error}</p>}<button className="btn btn-primary mt-5 w-full">Register</button><p className="mt-4 text-center text-sm muted">Sudah punya akun? <Link className="text-violet-300" href="/login">Login</Link></p></form></section>}
