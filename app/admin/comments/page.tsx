import { auth } from "@/lib/auth"; import { prisma } from "@/lib/prisma"; import { redirect } from "next/navigation"; export default async function AdminComments(){const s=await auth();if(s?.user.role!=="ADMIN")redirect("/");const comments=await prisma.comment.findMany({take:100,include:{user:true,anime:true,_count:{select:{likes:true}}},orderBy:{createdAt:"desc"}});return <section className="shell py-10"><h1 className="text-3xl font-black">Comment Moderation</h1><div className="mt-6 space-y-3">{comments.map(c=><article key={c.id} className="card p-4"><div className="flex justify-between"><b>{c.user.username} <span className="font-normal muted">on {c.anime.title}</span></b><span className="text-sm text-violet-300">{c.isPinned?"Pinned":"Normal"} · {c._count.likes} likes</span></div><p className="mt-2 text-sm text-gray-300">{c.content}</p></article>)}</div></section>}
