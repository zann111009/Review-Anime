import { auth } from "@/lib/auth"; import { prisma } from "@/lib/prisma"; import { redirect } from "next/navigation"; import Link from "next/link";
export default async function AdminAnime(){const s=await auth();if(s?.user.role!=="ADMIN")redirect("/");const list=await prisma.anime.findMany({take:100,orderBy:{updatedAt:"desc"}});return <section className="shell py-10"><h1 className="text-3xl font-black">Anime Management</h1><p className="mt-2 muted">The REST API supports create, update, and delete with Zod validation.</p><div className="mt-6 overflow-x-auto rounded-xl border border-white/10"><table className="w-full text-left text-sm"><thead className="bg-panel text-gray-300"><tr><th className="p-3">Title</th><th>Year</th><th>Status</th><th>Slug</th></tr></thead><tbody>{list.map(a=><tr key={a.id} className="border-t border-white/10"><td className="p-3"><Link className="text-violet-300" href={`/anime/${a.slug}`}>{a.title}</Link></td><td>{a.year}</td><td>{a.status}</td><td className="muted">{a.slug}</td></tr>)}</tbody></table></div></section>}
