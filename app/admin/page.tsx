import { auth } from "@/lib/auth"; import { prisma } from "@/lib/prisma"; import { redirect } from "next/navigation"; import Link from "next/link";
export default async function Admin(){const s=await auth();if(s?.user.role!=="ADMIN")redirect("/");const [anime,users,comments,ratings]=await Promise.all([prisma.anime.count(),prisma.user.count(),prisma.comment.count(),prisma.rating.count()]);return <section className="shell py-10"><div className="flex items-center justify-between"><div><h1 className="text-3xl font-black">Admin Dashboard</h1><p className="mt-1 muted">Manage AnimeRate content and community.</p></div><Link className="btn btn-primary" href="/admin/anime">Manage Anime</Link></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["Total Anime",anime],["Total Users",users],["Total Reviews",comments],["Total Ratings",ratings]].map(([name,value])=><div className="card p-5" key={String(name)}><p className="muted">{name}</p><b className="text-3xl">{value}</b></div>)}</div><div className="mt-8 glass rounded-xl p-6"><h2 className="font-bold">Quick links</h2><div className="mt-4 flex flex-wrap gap-3"><Link className="btn btn-ghost" href="/admin/anime">Anime CRUD</Link><Link className="btn btn-ghost" href="/admin/comments">Comment moderation</Link><Link className="btn btn-ghost" href="/genres">Genres</Link></div></div></section>}
