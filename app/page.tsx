import Link from "next/link"; import { prisma } from "@/lib/prisma"; import { AnimeGrid } from "@/components/AnimeGrid";
async function getAnime(){const data=await prisma.anime.findMany({take:12,include:{genres:{include:{genre:true}},ratings:{select:{score:true}},_count:{select:{ratings:true}}}});return data.map(a=>({...a,averageRating:a.ratings.length?a.ratings.reduce((s,r)=>s+Number(r.score),0)/a.ratings.length:0}));}
export default async function Home(){const anime=await getAnime();const top=[...anime].sort((a,b)=>b.averageRating-a.averageRating).slice(0,6);return <><section className="relative overflow-hidden border-b border-white/10 bg-gradient-to-br from-violet-950 via-ink to-ink py-24"><div className="shell relative max-w-4xl"><p className="mb-3 font-semibold text-violet-300">COMMUNITY-POWERED ANIME DISCOVERY</p><h1 className="text-4xl font-black leading-tight sm:text-6xl">Temukan Anime <span className="text-violet-400">Favoritmu.</span></h1><p className="mt-5 max-w-2xl text-lg muted">Berikan rating, tulis review, dan lihat anime terbaik berdasarkan komunitas AnimeRate.</p><div className="mt-8 flex flex-wrap gap-3"><Link href="/anime" className="btn btn-primary">Explore Anime</Link><Link href="/ranking" className="btn btn-ghost">Top Rated</Link></div></div></section><section className="shell py-12"><div className="mb-6 flex items-end justify-between"><div><h2 className="text-2xl font-bold">Trending Anime</h2><p className="muted">Pilihan komunitas minggu ini</p></div><Link href="/anime" className="text-sm text-violet-300">Lihat semua →</Link></div><AnimeGrid anime={anime.slice(0,10)}/></section><section className="shell py-4"><h2 className="mb-6 text-2xl font-bold">Top Rated Anime</h2><AnimeGrid anime={top}/></section></>}
