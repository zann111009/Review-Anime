import { redirect } from "next/navigation"; export default function Search(){async function go(f:FormData){"use server";redirect(`/anime?q=${encodeURIComponent(String(f.get("q")||""))}`)}return <section className="shell py-20"><h1 className="text-3xl font-black">Search Anime</h1><form action={go} className="mt-6 flex max-w-2xl gap-3"><input autoFocus name="q" className="field" placeholder="Title, alternative title..."/><button className="btn btn-primary">Search</button></form></section>}
