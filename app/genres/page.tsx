import Link from "next/link"; import { prisma } from "@/lib/prisma"; export default async function Genres(){const genres=await prisma.genre.findMany({include:{_count:{select:{anime:true}}},orderBy:{name:"asc"}});return <section className="shell py-10"><h1 className="text-3xl font-black">Genres</h1><p className="mt-2 muted">Explore anime by your favorite genre.</p><div className="mt-8 grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-5">{genres.map(g=><Link key={g.id} href={`/anime?genre=${g.slug}`} className="card p-5"><b>{g.name}</b><p className="mt-1 text-sm muted">{g._count.anime} anime</p></Link>)}</div></section>}
