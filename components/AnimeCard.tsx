import Link from "next/link"; import Image from "next/image"; import { Star } from "lucide-react";
export type AnimeCardData={id:string;slug:string;title:string;coverImage:string;year:number;status:string;genres:{genre:{name:string}}[];averageRating:number;_count:{ratings:number}};
export function AnimeCard({anime}:{anime:AnimeCardData}){return <Link href={`/anime/${anime.slug}`} className="card group"><div className="relative aspect-[2/3] overflow-hidden bg-panel"><Image src={anime.coverImage} alt={`Cover ${anime.title}`} fill className="object-cover transition duration-500" sizes="(max-width: 640px) 50vw, (max-width: 1024px) 33vw, 16vw"/><span className="absolute right-2 top-2 rounded-md bg-black/70 px-2 py-1 text-sm font-bold"><Star className="mr-1 inline text-yellow-400" size={14} fill="currentColor"/>{anime.averageRating.toFixed(1)}</span></div><div className="p-3"><h3 className="truncate font-bold">{anime.title}</h3><p className="mt-1 truncate text-xs muted">{anime.genres.slice(0,2).map(g=>g.genre.name).join(" • ")}</p><div className="mt-3 flex justify-between text-xs"><span className="text-violet-300">{anime.status}</span><span className="muted">{anime.year} · {anime._count.ratings} votes</span></div></div></Link>}
