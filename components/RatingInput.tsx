"use client"; import { useState } from "react"; import { Star } from "lucide-react";
export function RatingInput({animeId,initial}:{animeId:string;initial?:number}){const [score,setScore]=useState(initial??8);const [message,setMessage]=useState("");async function save(){const r=await fetch(`/api/anime/${animeId}/rating`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({score:Number(score.toFixed(1))})});setMessage(r.ok?"Rating berhasil disimpan.":"Silakan login terlebih dahulu.");}return <div className="glass rounded-xl p-4"><div className="mb-2 flex items-center gap-2 text-xl font-bold"><Star fill="currentColor" className="text-yellow-400"/> {score.toFixed(1)} / 10</div><input aria-label="Your rating" className="w-full accent-violet-500" type="range" min="1" max="10" step="0.1" value={score} onChange={e=>setScore(+e.target.value)}/><div className="mt-3 flex items-center justify-between"><span className="text-xs muted">Pilih nilai 1.0–10.0</span><button onClick={save} className="btn btn-primary">Save rating</button></div>{message&&<p className="mt-2 text-sm text-violet-300">{message}</p>}</div>}
